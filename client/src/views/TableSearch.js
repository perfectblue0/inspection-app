import { Navigation, SearchButton, TableDisplay } from "../components";

const TableSearch = () => {
  return (
    <div>
      <Navigation></Navigation>
      <SearchButton></SearchButton>
      <TableDisplay></TableDisplay>
    </div>
  );
};

export default TableSearch;
